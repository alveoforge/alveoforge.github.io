---
// Índice del blog en los idiomas con prefijo: '/es/blog/'.
import BlogIndex from '@/components/pages/BlogIndex.astro';
import { prefixedLocales } from '@/i18n/utils';

export function getStaticPaths() {
	return prefixedLocales.map((lang) => ({ params: { lang } }));
}
---

<BlogIndex />
