---
import LandingPage from '@/components/pages/LandingPage.astro';
import { prefixedLocales } from '@/i18n/utils';

// Solo los idiomas CON prefijo (todos menos el por defecto, que vive en '/').
export async function getStaticPaths() {
	return prefixedLocales.map((lang) => ({ params: { lang } }));
}
---

<LandingPage />
